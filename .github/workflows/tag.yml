name: Build & Tag
on:
  workflow_dispatch:
    inputs:
      dry-run:
        description: Build without publishing a tag
        default: true
        type: boolean
permissions:
  contents: write
jobs:
  release:
    # Replace BOTH references with an existing release tag or the same full commit SHA.
    uses: ccmjs/component-release/.github/workflows/release.yml@v2.1.0
    with:
      tooling-ref: v2.1.0
      dry-run: ${{ inputs.dry-run }}
